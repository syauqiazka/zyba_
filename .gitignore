.next
node_modules
.env
.env*.local
.history

# Local fallback data (berisi hash password demo, jangan pernah di-commit ke git)
data/*.json
data/
# Core dumps
core.*

# Uploaded files on self-hosted server
public/uploads/*
!public/uploads/.gitkeep
!public/uploads/avatars/
public/uploads/avatars/*
!public/uploads/avatars/.gitkeep
